# FROZEN

This directory is a **frozen experiment** (partial Rust port of `src/core`).

Do not add features, milestones, or training hooks here.

Training uses TypeScript `ai/ts` + `src/core`. See `ai/README.md`.
